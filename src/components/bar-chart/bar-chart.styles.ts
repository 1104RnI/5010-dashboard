import styled from 'styled-components'

export const BarChartContainer = styled.div`
	display: flex;
	flex-direction: column;
	gap: 2.5rem;
	padding-top: 1rem;

	div#data-pick-container {
		display: flex;
		flex-flow: row wrap;
		gap: 1.25rem;
	}
`
